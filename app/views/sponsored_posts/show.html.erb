<h1><%= @sponsored_post.title %></h1>

<div class="row">
  <div class="col-md-8">
    <p><%= @sponsored_post.price %></p>
    <p><%= @sponsored_post.body %></p>
  </div>
  test
  <div class="col-md-4">
 <%= link_to "Edit", edit_topic_post_path(@topic, post), class: 'btn btn-success' %>
    <%= link_to "Delete Post", @sponsored_post, method: :delete, class: 'btn btn-danger', data: {confirm: 'Are you sure you want to delete this sponsored post?'} %>
  </div>
</div>
